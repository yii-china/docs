# 018 — 小部件

与常规类不同，小部件用于视图模板中，因此语法很重要。
它应该既易于编写、易于阅读，又不太冗长，因为类似的构造旨在反复使用。

## 类名

小部件不使用后缀或前缀。

## 继承

允许继承，但有一些限制：

- 如果类不是抽象的，它应该是 final 的。
- 层次结构应尽可能保持线性。

## 不可变性

小部件应该：

- 没有状态。
- 是不可变的。

## 方法

### 名称

- 与其他类不同，返回具有某些属性修改的对象克隆的方法不使用前缀。
- 尽可能保持方法名称简短，但不要损害可读性。

### 布尔标志

对应于布尔属性的方法应以该属性命名，并接受布尔标志参数。
